# 결정 기록

이 문서는 구조 변경, 하네스 규칙 변경, 예외 승인을 기록한다. 새 기록은 최신 항목이 위에 오도록 추가한다.

## Entry Template

```markdown
## YYYY-MM-DD - [결정 제목]

- 결정:
- 이유:
- 영향:
- 승인:
- 검증:
```

## 2026-07-07 - MySQL 영구 저장 전환

- 결정: 앱 실행 기본 DB를 H2 메모리에서 로컬 MySQL `devlog` 데이터베이스로 전환하고, 자동 테스트는 `test` 프로필의 H2 메모리 DB를 사용한다.
- 이유: H2 메모리 DB는 백엔드 재시작 시 데이터가 사라지므로, 사용자가 프로젝트 재시작 후에도 데이터를 보존하기 위해 MySQL 영구 저장을 요청했다.
- 영향: `application.yml` 기본 datasource는 MySQL 환경변수 기반 설정을 사용하고, Gradle 테스트 태스크는 `spring.profiles.active=test`로 H2 설정을 사용한다.
- 승인: 사용자가 MySQL Workbench에서 `devlog` DB를 생성했으며, MySQL 방식으로 영구 저장을 진행하고 비밀번호는 직접 추가하겠다고 명시했다.
- 검증: `backend\gradlew.bat test --tests com.devlog.DatabaseConfigurationTest`와 `backend\gradlew.bat build`를 실행한다.

## 2026-07-07 - WorkLog TODO 상태명을 PLANNED로 변경

- 결정: 업무 일지 진행 상태의 `TODO` 값을 `PLANNED`로 변경한다.
- 이유: 별도 `Todo` 도메인의 할 일 항목과 업무 일지 상태값 `TODO`가 의미상 겹쳐 보이므로, 업무 일지의 예정/계획 상태는 `PLANNED`로 명확히 구분한다.
- 영향: 백엔드 `WorkLogStatus`, 프론트엔드 `WorkLogStatus` 타입과 업무 일지 기본값, MVP 스펙 문서의 상태 목록이 `PLANNED` 기준으로 동작한다.
- 승인: 사용자가 `PLANNED로 바꿔줘!`라고 명시적으로 요청했다.
- 검증: WorkLog Service 테스트, 프론트엔드 API 테스트, 백엔드 전체 테스트/빌드, 프론트엔드 전체 테스트/빌드를 실행한다.

## 2026-07-07 - Controller 테스트 패키지 허용

- 결정: HTTP 요청/응답, 예외 처리, 입력값 검증 실패 테스트를 위해 `backend/src/test/java/com/devlog/controller` 테스트 패키지를 허용한다.
- 이유: Service 테스트만으로는 `@Valid`, ControllerAdvice, HTTP 상태 코드, JSON 에러 응답 형식을 직접 검증할 수 없기 때문이다.
- 영향: 작업자는 Controller 경계 테스트를 `com.devlog.controller` 테스트 패키지에 추가할 수 있다.
- 승인: 사용자가 예외 처리와 입력값 검증 실패 테스트 보강을 요청했다.
- 검증: Controller MVC 테스트와 전체 백엔드 빌드를 실행한다.

## 2026-07-07 - README와 5단계 점검 체크리스트 작성

- 결정: 루트 `README.md`를 현재 MVP 상태와 실행/검증 방법 중심으로 보강하고, 5단계 완료 판단용 `10-phase-5-checklist.md`를 추가한다.
- 이유: 1-4단계 구현 후 남은 5단계 항목을 예외 처리, 입력값 검증, 테스트 보강, README 최신화 기준으로 명확히 점검하기 위해서다.
- 영향: 작업자는 5단계 마무리나 품질 점검을 수행할 때 `10-phase-5-checklist.md`를 확인한다.
- 승인: 사용자가 README 작성과 5단계 점검 체크리스트 작성을 요청했다.
- 검증: README와 하네스 문서 링크, 문서 diff, 관련 검색 결과를 확인한다.

## 2026-07-07 - DevLog MVP 제품 기준 문서화

- 결정: DevLog의 MVP 기능 목록, API 목록, Entity 후보와 관계, 기술 세부 기준, 1-5단계 구현 순서를 `09-devlog-mvp-spec.md`에 문서화한다.
- 이유: 긴 초기 프롬프트의 제품 기준이 하네스에 상세히 남아 있지 않으면 이후 작업자가 구현 범위와 제외 범위를 반복해서 재해석해야 하기 때문이다.
- 영향: 기능 구현 전 작업자는 `09-devlog-mvp-spec.md`에서 해당 MVP 항목과 단계 순서를 확인해야 한다.
- 승인: 사용자가 MVP 기능 목록, API 목록, Entity 후보, 세부 기준, 1-5단계 구현 순서를 md 파일에 상세 반영하라고 요청했다.
- 검증: 하네스 인덱스와 작업 흐름 계약에서 새 문서가 연결되는지 확인한다.

## 2026-07-07 - 실패 수정 로그 문서 추가

- 결정: 예상하지 못한 테스트, 빌드, 검증 실패를 수정한 이력을 `08-failure-fix-log.md`에 별도로 기록한다.
- 이유: `07-decision-log.md`는 구조와 규칙 결정 기록용이므로, 실패 원인과 수정 이력을 분리해 추적하기 위해서다.
- 영향: 작업자는 실패를 수정한 뒤 증상, 원인, 수정 내용, 재발 방지 검증을 실패 수정 로그에 남긴다.
- 승인: 사용자가 실패한 내용을 수정하면 기록하는 md 파일이 있으면 좋겠다고 요청했다.
- 검증: 하네스 인덱스, 작업 흐름 계약, 검증 체크리스트에서 새 문서가 연결되는지 확인한다.

## 2026-07-07 - TDD 기반 바이브 프로젝트 운영 원칙 명시

- 결정: DevLog를 바이브 프로젝트로 명시하고, 기능 추가와 버그 수정은 RED-GREEN-REFACTOR 순서의 TDD 흐름으로 진행하도록 하네스 문서를 갱신한다.
- 이유: 빠른 AI 협업 프로젝트일수록 테스트가 먼저 실패하고 최소 구현으로 통과하는 절차가 있어야 변경 의도와 회귀 방지를 확인할 수 있기 때문이다.
- 영향: 작업자는 코드 변경 전 실패 테스트, 기대 실패 이유, 통과 확인 명령을 스펙과 구현 계획에 기록해야 한다.
- 승인: 사용자가 md 파일을 분석해 TDD 방식으로 진행할 수 있게 변경하고, 프로젝트가 바이브 프로젝트임을 명시해 달라고 요청했다.
- 검증: 문서 링크와 하네스 흐름을 확인하고, 문서 변경만 있는 경우 코드 빌드는 생략할 수 있다.

## 2026-07-07 - 3단계 React 프론트엔드 모듈 도입

- 결정: 루트에 `frontend` Vite React TypeScript 애플리케이션을 추가하고, 하네스 파일 구조 계약과 검증 체크리스트에 프론트엔드 구조와 `npm run build` 검증을 반영한다.
- 이유: DevLog 3단계 범위인 라우팅, 사이드바 레이아웃, Project/WorkLog 화면을 백엔드와 분리된 프론트엔드 실행 모듈로 구현하기 위해서다.
- 영향: 작업자는 프론트엔드 변경 시 `frontend/src` 구조 계약을 따르고, 완료 전 `npm run build`를 실행해야 한다.
- 승인: 사용자가 3단계 React 프론트엔드 구현 계획 실행을 명시적으로 요청했다.
- 검증: `backend/gradlew.bat build`와 `frontend/npm run build`를 실행한다.

## 2026-07-07 - AI 작업 하네스 0단계 도입

- 결정: 루트 `AGENTS.md`와 `docs/harness` 문서 세트를 추가해 AI 작업자가 따라야 할 문서 기반 하네스를 만든다.
- 이유: 이후 코드 변경이 임의의 구조 변경으로 번지지 않도록 작업 전 확인 문서, 구조 계약, 검증 기준을 고정하기 위해서다.
- 영향: 작업자는 코드 수정 전 하네스 인덱스와 관련 계약 문서를 읽어야 한다.
- 승인: 사용자가 AI 작업 하네스 방식의 0단계 MD 구조 계획 구현을 요청했다.
- 검증: 문서 링크, 현재 백엔드 구조와의 일치 여부, 사용 시나리오를 확인한다.
